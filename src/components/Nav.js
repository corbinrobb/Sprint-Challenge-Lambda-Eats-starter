import React from 'react';
import { Link } from 'react-router-dom';

const Nav = () => {
  return (
    <nav>
      <Link className="App-link" to="/">Home</Link>
    </nav>
  );
}

export default Nav;